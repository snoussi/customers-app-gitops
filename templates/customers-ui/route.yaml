apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: {{ include "customersui.fullname" . }}
  labels:
    {{- include "customersui.labels" . | nindent 4 }}
    router: internal
spec:
  {{- if .host }}
  host: {{ .host }}
  {{- end }}
  {{- if .path }}
  path: {{ .path }}
  {{- end }}
  port:
    targetPort: 80
  to:
    kind: Service
    name: {{ include "customersui.fullname" . }}
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None